
            export // Calculator 클래스는 역할을 담당합니다.
class Calculator {
                add(a: number, b: number) {
                    if (a > 0) return a + b;
                    return b;
                }
            }
            export // unusedFunction 함수는 역할을 수행합니다.
function unusedFunction() {
                return 'I am dead';
            }
        